/*

    react是什么:

        一个构建用户界面的js库

    react的特点: 

        声明式
        组件化
        一次学习,随处编写

    react的使用:

        1. 引入js库
            react.js   React
            react-dom.js  ReactDOM
            babel.js 
        
        2. 准备根标签 

        3. const 虚拟dom = React.createElement()
        4. ReactDOM.render(虚拟dom, 根标签dom对象)


        // 使用React.createElement()创建html结构,比较繁琐.所以使用jsx 
        const 虚拟dom = <h1 className="类名">子节点</h1>
        // 注意: jsx必须只能有一个根标签, script需要写type="text/babel"

        在jsx中插入数据: 
            写插值表达式: {} 
            书写的位置: 1. 子节点 2. 属性的值 
            写入的内容: js中任意的表达式














*/
